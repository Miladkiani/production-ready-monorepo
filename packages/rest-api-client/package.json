{
  "name": "@repo/rest-api-client",
  "version": "0.1.0",
  "private": true,
  "description": "Planned typed REST API client for the monorepo backend. Source not yet implemented — see README.",
  "scripts": {
    "build": "tsc",
    "check-types": "tsc --noEmit"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "typescript": "^5"
  }
}
