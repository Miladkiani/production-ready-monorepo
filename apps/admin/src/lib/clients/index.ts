/**
 * GraphQL client wrapper
 */

export { executeGraphQL } from "./graphql-client";
export type { ExecuteGraphQLOptions } from "./graphql-client";

// Server-side GraphQL client (for Server Components only)
export { executeServerGraphQL } from "./graphql-server";
